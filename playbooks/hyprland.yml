---
- name: Install and configure hyprland as Wayland compositor
  hosts: localhost
  become: yes  # Use sudo for administrative tasks

  tasks:
    - name: Install hyprland
      package:
        name: hyprland
        state: present
