---
- name: Main playbook using include_tasks
  hosts: localhost
  become: yes
  tasks:
    - name: Ensure git is installed
      pacman:
        name: git
        state: present

    - name: Create a directory for the playbooks
      file:
        path: "~/dotfiles_playbooks"
        state: directory

    - name: Clone dotfiles repository
      git:
        repo: 'https://github.com/CEnjolras/dotfiles.git'
        dest: '~/dotfiles_playbooks'
        depth: 1
        force: yes

    - name: Include pacman_reflector tasks
      include_tasks: '~/dotfiles_playbooks/playbooks/pacman_reflector.yml'

    - name: Include yay tasks
      include_tasks: '~/dotfiles_playbooks/playbooks/yay.yml'

    - name: Include terminal_fish_foot tasks
      include_tasks: '~/dotfiles_playbooks/playbooks/terminal_fish_foot.yml'

    - name: Include nvm tasks
      include_tasks: '~/dotfiles_playbooks/playbooks/nvm.yml'

    - name: Include hyprland tasks
      include_tasks: '~/dotfiles_playbooks/playbooks/hyprland.yml'

    - name: Cleanup - Remove the dotfiles_playbooks directory
      file:
        path: "~/dotfiles_playbooks"
        state: absent
